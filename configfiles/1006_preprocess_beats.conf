filter {
    if [source] =~ "^\/nsm\/bro" {
        grok {
            match => { "source" => "\/%{WORD:file_name}\.log$" }
        }
        mutate {
            replace => { "type" => "bro_%{file_name}" }
            remove_field => "file_name"
            add_tag => "bro"
        }
    }
}